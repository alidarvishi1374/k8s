apiVersion: v1
kind: Service
metadata:
  name: proxy-svc
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: proxy

  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}